apiVersion: v1
kind: Service
metadata:
  name: {{ include "qdrant-es-gateway.fullname" . }}
  labels:
    {{- include "qdrant-es-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "qdrant-es-gateway.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
